

## Update AI-assisted support text

### Change — `src/pages/Auth.tsx` (line 713)

Replace the current single-sentence description with the new two-sentence copy:

> "When setting up, you teach the system by uploading your organisation's certificates — the AI learns your certificate landscape and establishes types automatically. From there, AI extracts data from new uploads and a chatbot assists with certificate handling, while keeping a human in control."

Single line edit, text only.

