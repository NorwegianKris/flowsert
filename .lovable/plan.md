

## Disable Email Confirmation for Signups

I will use the auth configuration tool to enable auto-confirm for email signups. This means new users can log in immediately after registering without verifying their email.

**Change:**
- Configure auth settings to set `autoconfirm: true` for email signups

No code changes are needed — this is a backend configuration change only.

