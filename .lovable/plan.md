

## Update AI-assisted support text

### Change — `src/pages/Auth.tsx` (line 713)

Replace the current description with:

> "You teach the system during setup by uploading your organisation's certificates — the AI establishes types automatically. From there, it extracts data from new uploads and a chatbot assists with certificate handling, while keeping a human in control."

Single line edit, text only.

