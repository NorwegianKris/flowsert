

## Add Norwegian Company Text to Footer

### Change — `src/components/PublicFooter.tsx`

Add a centered row between the copyright and nav links containing "We are a Norwegian company! 🇳🇴" in slightly larger text (`text-base` vs the existing `text-sm`). Insert a new `div` in the middle of the flex layout with `text-center` alignment and `text-muted-foreground` styling to match the footer aesthetic.

### File
- `src/components/PublicFooter.tsx`

