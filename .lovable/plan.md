

## Three Small Text Styling Updates in `src/pages/Auth.tsx`

### Changes

1. **Bullet point labels** (lines 582-587): Change `font-medium` to `font-bold` on all 6 `<span>` elements
2. **"FlowSert" bold** (line 590): Wrap "FlowSert" in `<span className="font-bold text-foreground">FlowSert</span>`
3. **Closing statement** (line 592): Change `font-medium` to `font-bold`

Single file, text styling only.

