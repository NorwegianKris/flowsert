

## Worker Groups: Full Feature + Admin Filter — IMPLEMENTED

All phases complete:
- Phase 1: Database migration (worker_groups + personnel_worker_groups, admin-only RLS, no FOR ALL)
- Phase 2: CategoriesSection renamed "Roles" → "Workers" with sub-tabs for Roles + Worker Groups
- Phase 3: WorkerGroupsManager, WorkerGroupMergingPane, WorkerGroupsManageList created
- Phase 4: useWorkerGroups, usePersonnelWorkerGroups, usePersonnelGroupFilter hooks created
- Phase 5: PersonnelFilters + AdminDashboard updated with Worker Groups filter + ghost pruning
